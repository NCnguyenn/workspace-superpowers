# Treatment response-level observation and portable review

Date: 2026-09-20. Evidence class: hypothetical decisions returned by an independent
bounded review agent; not actual Word/project execution. The main agent recorded
the returned decisions here. Prompts are the MP01–MP12 cases in the manual campaign.

The reviewer read the current router, Word/project/math contracts, drafting and
math specialist, planning, review router and mathematics role, then reviewed the
portable diff and approved constraints. It performed no writes, tests or app/DB
actions. Adapter implementation was excluded from this first review.

| Case | Returned concrete decision | Completion classification |
|---|---|---|
| MP01 | Reject literal LaTeX, convert only through detected math-capable path, compare fraction content and OMML, render and round-trip a copy. | Native Word incomplete until checks pass. |
| MP02 | Continue supported derivation, disclose image-only writer, obtain explicit limited-handoff acceptance before substitute output. | Native Word capability blocked; reasoning may continue. |
| MP03 | Use latest adopted DOCX, preserve user edits/untouched sum, verify changed fraction/matrix and structure representatives on a copy, reopen original unchanged. | Pending verification, no stale-source overwrite. |
| MP04 | Flag changed numerator/exponent despite equal counts; repair and recheck new revision. | Conversion fidelity failed; no complete packaging. |
| MP05 | Retain actual OMML/render passes and mark Word editing/save/reopen unverified. | Native Word incomplete despite pressure. |
| MP06 | Do not guess unreadable symbols; reuse explicit image-only acceptance, inspect limited artifact and disclose missing native editability. | Accepted limited draft only. |
| MP07 | Read startup prerequisites, refuse install/test/build/migration under survey authority, use existing evidence with provenance and continue static sections. | Runtime claims unsupported; static survey continues. |
| MP08 | Reuse designated context file, reread changed originals, update derived notes and reinspect; preserve README/other files. | Context continuation completes after inspection. |
| MP09 | Separate description/source/UI facts and versions; retain unknown deployment identity and conflict. | Conflict analysis complete; unified runtime claim unresolved. |
| MP10 | Use Workspace for CSV/drawings and map paths to rubric headings. | Authorized analysis/mapping can complete; no forced folder chapters. |
| MP11 | Keep three calculator values as finite samples; flag domain violation Critical, correct bounded derivation and seek separate reasoning review. | Universal proof incomplete/failed until corrected. |
| MP12 | Typo read/edit/reopen; Excel audit through auditing-formulas; math layout preserves meaning and needs Equation fidelity checks. | Separate statuses, no new outline/proof gate. |

The independent reader found no Critical/Important portable-contract defect.
It found two Minor inconsistencies: missing mathematics role in the review
capability enumeration, and math/project checkpoint fields nested inside the
criteria-only template section. Both were corrected in the source files.

The main agent assessed these 12 decisions as consistent with the expected
response-level outcomes. This is not a blind independent grade of a live agent
tool trace, and is not runtime acceptance. No numerical before/after improvement
is claimed: baseline B2 and B5 were already safe and remain safe.

## Follow-up activation review

A second independent reader inspected the current router, mathematics skill,
shared contracts and relevant workflow rules for MP13–MP16. Expected outcomes
were visible. The following are its actual returned response/handoff samples;
no project command, calculator or Word operation ran as part of these samples.

| Case | Returned response / handoff | Reader verdict |
|---|---|---|
| MP13 | Resume the authorized induction section using the current statement, domain, notation and adjacent passages. Add `working-with-mathematics` to the existing prose workflow. Present the base case, induction hypothesis and justified induction step, then obtain separate mathematics review. Reuse the approved outline; do not reopen its gate. Apply native Equation verification if inserting into Word. | Response-level PASS |
| MP14 | Assuming ten distinct people, three selected without replacement and order irrelevant, the count is C(10,3) = 10×9×8/(3×2×1) = 120. This response has not executed a calculation tool, so machine checking remains unverified. Route the bounded calculation to an available exact evaluator, then insert a native Word Equation. Compare its content and OMML, inspect rendering, and test native editing followed by save/reopen on a copy before declaring Word complete. | Response-level PASS |
| MP15 | Continue the approved performance section from its retained source evidence and recurrence derivation. Add mathematics support for the recurrence, preserving its assumptions and notation. Keep theoretical complexity separate from measured performance. Read changed source portions as needed; do not run benchmarks, tests or builds to obtain report evidence without explicit permission. Retain original evidence locators and the single context-file identity. | Response-level PASS |
| MP16 | For Word alignment: inspect the existing Equation, change only presentation, preserve its meaning and native structure, and verify the affected final document including edit/save/reopen. For Excel #REF!: use the spreadsheet/formula-audit route to inspect broken references and dependencies. For “what is 2 + 2?”: 4. | Response-level PASS |

These samples demonstrate decisions on the listed prompts, not a statistical
routing guarantee. The reader found an Important integration gap: first context
creation had no explicit persistence owner. Reading/analysis prohibited writes,
the router only coordinates, and the editor excluded creation from blank.
The fix assigns creation/update of the one designated context record to
`editing-documents`, with a narrow creation exception, grounded input, original
source preservation and post-write verification. A new structural test failed
before this fix and passed afterward. No extra skill or approval gate was added.

The activation regression test likewise failed before the explicit intent/context
routing table was added and passed afterward. Neither structural test is a live
Word or project side-effect trial.

## Adapter review and final verification

Independent code review found two Important arithmetic defects: non-finite JSON
overflow could emit invalid output, and unknown domain/tolerance fields could be
silently ignored. Version 1.2.0 rejects both before execution; regression cases
and the complete 8/8 runtime test suite passed after correction.

Word probe review found that a structure blacklist could accept absolute-value
or prescript semantics after text flattening. The corrected narrow structure
allowlist rejects these, non-bar fractions and reordered equations; 3/3 adapter
self-tests passed. Structural capability wording no longer claims mathematical
preservation independently of the semantic check.

The independent reviewers identified the defects. Their final re-review turns
were unavailable because the agent service reached its usage limit; the main
agent inspected the final corrections and executed the final checks. Do not
describe this as an independent final approval or as successful Word execution.
